-- local _vim = vim

-- if (1) then
--     _vim.cmd('hi Normal guibg=NONE ctermbg=NONE');
--     _vim.cmd('hi EndOfBuffer guibg=NONE ctermbg=NONE');
-- else
    -- define used colorscheme

    -- local is_ok, _ = pcall(_vim.cmd, "colorscheme " .. colorscheme)
    -- if not is_ok then
    --     _vim.notify("colorscheme " .. colorscheme .. " is not found!");
    --     return
    -- end
-- end
